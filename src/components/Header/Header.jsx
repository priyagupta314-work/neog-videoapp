import React from "react";

export default function Header(){
    return (
        <nav className="Header">
             <button className="btn btn-primary btn-nav">Login</button>
      
      </nav>
    )
}